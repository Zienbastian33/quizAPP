<div class="min-h-screen flex items-center justify-center bg-gray-50 px-4">
  <div class="max-w-md w-full bg-white rounded-xl shadow-lg p-8">
    <div class="text-center mb-8">
      <h1 class="text-3xl font-bold text-gray-900">🧠 QuizApp</h1>
      <h2 class="text-xl text-gray-600 mt-2">Editar Perfil</h2>
    </div>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name),
        html: { method: :put, class: "space-y-5" }) do |f| %>

      <%= render "devise/shared/error_messages", resource: resource %>

      <div>
        <%= f.label :name, "Nombre", class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.text_field :name, autofocus: true, autocomplete: "name",
            class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition" %>
      </div>

      <div>
        <%= f.label :email, class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.email_field :email, autocomplete: "email",
            class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition" %>
      </div>

      <div class="border-t border-gray-200 pt-5">
        <p class="text-sm text-gray-500 mb-3">Dejar en blanco si no deseas cambiar la contraseña</p>

        <div class="space-y-4">
          <div>
            <%= f.label :password, "Nueva contraseña", class: "block text-sm font-medium text-gray-700 mb-1" %>
            <%= f.password_field :password, autocomplete: "new-password",
                placeholder: "••••••••",
                class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition" %>
            <% if @minimum_password_length %>
              <p class="text-xs text-gray-400 mt-1">Mínimo <%= @minimum_password_length %> caracteres</p>
            <% end %>
          </div>

          <div>
            <%= f.label :password_confirmation, "Confirmar nueva contraseña",
                class: "block text-sm font-medium text-gray-700 mb-1" %>
            <%= f.password_field :password_confirmation, autocomplete: "new-password",
                placeholder: "••••••••",
                class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition" %>
          </div>
        </div>
      </div>

      <div class="border-t border-gray-200 pt-5">
        <div>
          <%= f.label :current_password, "Contraseña actual",
              class: "block text-sm font-medium text-gray-700 mb-1" %>
          <p class="text-xs text-gray-400 mb-1">Necesaria para confirmar los cambios</p>
          <%= f.password_field :current_password, autocomplete: "current-password",
              placeholder: "••••••••",
              class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition" %>
        </div>
      </div>

      <div class="flex gap-3 pt-2">
        <%= f.submit "Guardar Cambios",
            class: "flex-1 bg-indigo-600 text-white py-3 px-4 rounded-lg font-semibold hover:bg-indigo-700 focus:ring-4 focus:ring-indigo-200 transition cursor-pointer" %>
        <%= link_to "Cancelar", root_path,
            class: "flex-1 text-center bg-gray-100 text-gray-700 py-3 px-4 rounded-lg font-medium hover:bg-gray-200 transition" %>
      </div>
    <% end %>

    <div class="mt-8 pt-5 border-t border-gray-200 text-center">
      <p class="text-sm text-gray-500 mb-2">¿Deseas eliminar tu cuenta?</p>
      <%= button_to "Eliminar mi cuenta", registration_path(resource_name),
          method: :delete,
          class: "text-red-500 hover:text-red-700 text-sm font-medium",
          data: { turbo_confirm: "¿Estás seguro? Esta acción no se puede deshacer." } %>
    </div>
  </div>
</div>
