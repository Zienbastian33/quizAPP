<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "QuizApp Admin" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <%# ══ Admin Navbar ══ %>
    <nav class="bg-indigo-700 text-white px-6 py-3 flex justify-between items-center">
      <div class="flex items-center gap-4">
        <%= link_to "🧠 QuizApp Admin", admin_root_path, class: "font-bold text-lg hover:text-indigo-200" %>
        <%= link_to "Quizzes", admin_quizzes_path, class: "text-indigo-200 hover:text-white text-sm" %>
      </div>
      <div class="flex items-center gap-4 text-sm">
        <span class="text-indigo-200"><%= current_user.name %> (<%= current_user.role %>)</span>
        <%= link_to "Ir al sitio", root_path, class: "text-indigo-200 hover:text-white" %>
        <%= link_to "Cerrar Sesión", destroy_user_session_path, data: { turbo_method: :delete },
            class: "bg-indigo-600 px-3 py-1 rounded hover:bg-indigo-500" %>
      </div>
    </nav>

    <%# ══ Flash Messages ══ %>
    <% if notice.present? %>
      <div class="max-w-4xl mx-auto mt-4 px-4">
        <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-lg flex justify-between items-center"
             data-controller="flash" data-flash-target="message">
          <p><%= notice %></p>
          <button data-action="click->flash#dismiss" class="text-green-600 hover:text-green-800 font-bold">&times;</button>
        </div>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="max-w-4xl mx-auto mt-4 px-4">
        <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-lg flex justify-between items-center"
             data-controller="flash" data-flash-target="message">
          <p><%= alert %></p>
          <button data-action="click->flash#dismiss" class="text-red-600 hover:text-red-800 font-bold">&times;</button>
        </div>
      </div>
    <% end %>

    <main class="container mx-auto mt-8 px-5">
      <%= yield %>
    </main>
  </body>
</html>
