<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "QuizApp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full flex flex-col bg-gray-50">
    <%# ══ Navbar ══ %>
    <nav class="bg-white border-b border-gray-200 sticky top-0 z-50" data-controller="navbar">
      <div class="max-w-6xl mx-auto px-4 py-3 flex justify-between items-center">
        <div class="flex items-center gap-6">
          <%= link_to "🧠 QuizApp", root_path, class: "text-xl font-bold text-gray-900 hover:text-indigo-600 transition" %>
          <%= link_to "Quizzes", quizzes_path, class: "hidden sm:inline text-gray-600 hover:text-gray-900 text-sm font-medium transition" %>
        </div>

        <%# Botón hamburguesa (solo móvil) %>
        <button class="sm:hidden text-gray-600 hover:text-gray-900"
                data-action="click->navbar#toggle"
                aria-label="Menú">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
        </button>

        <%# Links desktop %>
        <div class="hidden sm:flex items-center gap-4">
          <% if user_signed_in? %>
            <% if current_user.admin? %>
              <%= link_to "Panel Admin", admin_root_path,
                  class: "text-indigo-600 hover:text-indigo-800 text-sm font-medium transition" %>
            <% end %>
            <%= link_to current_user.name, edit_user_registration_path,
                class: "text-sm text-gray-500 hover:text-gray-700 transition" %>
            <%= link_to "Salir", destroy_user_session_path, data: { turbo_method: :delete },
                class: "text-gray-400 hover:text-gray-600 text-sm transition" %>
          <% else %>
            <%= link_to "Iniciar Sesión", new_user_session_path,
                class: "text-gray-600 hover:text-gray-900 text-sm font-medium transition" %>
            <%= link_to "Registrarse", new_user_registration_path,
                class: "bg-indigo-600 text-white px-4 py-2 rounded-lg hover:bg-indigo-700 text-sm font-medium transition" %>
          <% end %>
        </div>
      </div>

      <%# Menú móvil (oculto por defecto) %>
      <div class="sm:hidden hidden border-t border-gray-100 bg-white px-4 py-3 space-y-2"
           data-navbar-target="menu">
        <%= link_to "Quizzes", quizzes_path, class: "block text-gray-600 hover:text-gray-900 text-sm font-medium py-1" %>
        <% if user_signed_in? %>
          <% if current_user.admin? %>
            <%= link_to "Panel Admin", admin_root_path, class: "block text-indigo-600 text-sm font-medium py-1" %>
          <% end %>
          <div class="border-t border-gray-100 pt-2 mt-2">
            <%= link_to current_user.name, edit_user_registration_path,
                class: "block text-sm text-gray-500 hover:text-gray-700 py-1" %>
            <%= link_to "Cerrar Sesión", destroy_user_session_path, data: { turbo_method: :delete },
                class: "block text-red-500 text-sm font-medium py-1" %>
          </div>
        <% else %>
          <div class="border-t border-gray-100 pt-2 mt-2">
            <%= link_to "Iniciar Sesión", new_user_session_path,
                class: "block text-gray-600 text-sm font-medium py-1" %>
            <%= link_to "Registrarse", new_user_registration_path,
                class: "block text-indigo-600 text-sm font-medium py-1" %>
          </div>
        <% end %>
      </div>
    </nav>

    <%# ══ Flash Messages ══ %>
    <% if notice.present? %>
      <div class="max-w-4xl mx-auto mt-4 px-4" data-controller="flash">
        <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-lg flex justify-between items-center"
             data-flash-target="message">
          <div class="flex items-center gap-2">
            <span>✓</span>
            <p><%= notice %></p>
          </div>
          <button data-action="click->flash#dismiss" class="text-green-600 hover:text-green-800 font-bold text-lg leading-none">&times;</button>
        </div>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="max-w-4xl mx-auto mt-4 px-4" data-controller="flash">
        <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-lg flex justify-between items-center"
             data-flash-target="message">
          <div class="flex items-center gap-2">
            <span>⚠</span>
            <p><%= alert %></p>
          </div>
          <button data-action="click->flash#dismiss" class="text-red-600 hover:text-red-800 font-bold text-lg leading-none">&times;</button>
        </div>
      </div>
    <% end %>

    <%# ══ Contenido principal ══ %>
    <main class="flex-1">
      <%= yield %>
    </main>

    <%# ══ Footer ══ %>
    <footer class="bg-white border-t border-gray-200 mt-auto">
      <div class="max-w-6xl mx-auto px-4 py-6">
        <div class="flex flex-col sm:flex-row justify-between items-center gap-4">
          <div class="flex items-center gap-2 text-sm text-gray-500">
            <span>🧠</span>
            <span>QuizApp — Prueba Técnica RioLabs</span>
          </div>
          <div class="flex items-center gap-4 text-sm text-gray-400">
            <span>Ruby on Rails <%= Rails.version %></span>
            <span>•</span>
            <span>Ruby <%= RUBY_VERSION %></span>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
