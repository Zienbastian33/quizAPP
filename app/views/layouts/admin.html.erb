<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "QuizApp Admin" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full flex flex-col bg-gray-100">
    <%# ══ Admin Navbar ══ %>
    <nav class="bg-indigo-700 text-white sticky top-0 z-50" data-controller="navbar">
      <div class="max-w-6xl mx-auto px-4 py-3 flex justify-between items-center">
        <div class="flex items-center gap-4">
          <%= link_to "🧠 QuizApp Admin", admin_root_path, class: "font-bold text-lg hover:text-indigo-200 transition" %>
          <%= link_to "Dashboard", admin_root_path, class: "hidden sm:inline text-indigo-200 hover:text-white text-sm transition" %>
          <%= link_to "Quizzes", admin_quizzes_path, class: "hidden sm:inline text-indigo-200 hover:text-white text-sm transition" %>
        </div>

        <%# Botón hamburguesa (solo móvil) %>
        <button class="sm:hidden text-indigo-200 hover:text-white"
                data-action="click->navbar#toggle"
                aria-label="Menú">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
        </button>

        <%# Links desktop %>
        <div class="hidden sm:flex items-center gap-4 text-sm">
          <span class="text-indigo-200"><%= current_user.name %></span>
          <%= link_to "Ir al sitio", root_path, class: "text-indigo-200 hover:text-white transition" %>
          <%= link_to "Cerrar Sesión", destroy_user_session_path, data: { turbo_method: :delete },
              class: "bg-indigo-600 px-3 py-1.5 rounded-lg hover:bg-indigo-500 transition" %>
        </div>
      </div>

      <%# Menú móvil admin %>
      <div class="sm:hidden hidden border-t border-indigo-600 bg-indigo-700 px-4 py-3 space-y-2"
           data-navbar-target="menu">
        <%= link_to "Dashboard", admin_root_path, class: "block text-indigo-200 hover:text-white text-sm py-1" %>
        <%= link_to "Quizzes", admin_quizzes_path, class: "block text-indigo-200 hover:text-white text-sm py-1" %>
        <div class="border-t border-indigo-600 pt-2 mt-2">
          <%= link_to "Ir al sitio", root_path, class: "block text-indigo-200 hover:text-white text-sm py-1" %>
          <span class="block text-indigo-300 text-sm py-1"><%= current_user.name %></span>
          <%= link_to "Cerrar Sesión", destroy_user_session_path, data: { turbo_method: :delete },
              class: "block text-red-300 hover:text-red-200 text-sm py-1" %>
        </div>
      </div>
    </nav>

    <%# ══ Flash Messages ══ %>
    <% if notice.present? %>
      <div class="max-w-4xl mx-auto mt-4 px-4" data-controller="flash">
        <div class="bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-lg flex justify-between items-center"
             data-flash-target="message">
          <div class="flex items-center gap-2">
            <span>✓</span>
            <p><%= notice %></p>
          </div>
          <button data-action="click->flash#dismiss" class="text-green-600 hover:text-green-800 font-bold text-lg leading-none">&times;</button>
        </div>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="max-w-4xl mx-auto mt-4 px-4" data-controller="flash">
        <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-lg flex justify-between items-center"
             data-flash-target="message">
          <div class="flex items-center gap-2">
            <span>⚠</span>
            <p><%= alert %></p>
          </div>
          <button data-action="click->flash#dismiss" class="text-red-600 hover:text-red-800 font-bold text-lg leading-none">&times;</button>
        </div>
      </div>
    <% end %>

    <%# ══ Contenido principal ══ %>
    <main class="flex-1 max-w-6xl mx-auto w-full mt-8 px-4 pb-8">
      <%= yield %>
    </main>

    <%# ══ Footer admin ══ %>
    <footer class="bg-gray-200 border-t border-gray-300 mt-auto">
      <div class="max-w-6xl mx-auto px-4 py-4">
        <div class="flex flex-col sm:flex-row justify-between items-center gap-2 text-xs text-gray-500">
          <span>QuizApp Admin Panel — RioLabs</span>
          <span>Rails <%= Rails.version %> • Ruby <%= RUBY_VERSION %></span>
        </div>
      </div>
    </footer>
  </body>
</html>
