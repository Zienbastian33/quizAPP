<div class="max-w-4xl mx-auto px-4 py-12">
  <h1 class="text-4xl font-bold text-gray-900 mb-4">🧠 QuizApp</h1>
  <p class="text-xl text-gray-600 mb-8">Plataforma de quizzes interactivos</p>

  <div class="flex gap-4">
    <% if user_signed_in? %>
      <% if current_user.admin? %>
        <%= link_to "Panel Admin", admin_root_path,
            class: "bg-indigo-600 text-white px-6 py-3 rounded-lg hover:bg-indigo-700" %>
      <% else %>
        <%= link_to "Ver Quizzes", quizzes_path,
            class: "bg-indigo-600 text-white px-6 py-3 rounded-lg hover:bg-indigo-700" %>
      <% end %>
      <%= link_to "Cerrar Sesión", destroy_user_session_path, data: { turbo_method: :delete },
          class: "bg-gray-200 text-gray-700 px-6 py-3 rounded-lg hover:bg-gray-300" %>
    <% else %>
      <%= link_to "Iniciar Sesión", new_user_session_path,
          class: "bg-indigo-600 text-white px-6 py-3 rounded-lg hover:bg-indigo-700" %>
      <%= link_to "Registrarse", new_user_registration_path,
          class: "bg-gray-200 text-gray-700 px-6 py-3 rounded-lg hover:bg-gray-300" %>
    <% end %>
  </div>

  <% if user_signed_in? %>
    <div class="mt-8 p-4 bg-blue-50 border border-blue-200 rounded-lg">
      <p class="text-blue-800">
        Sesión activa: <strong><%= current_user.name %></strong>
        (<%= current_user.role %>)
        — <%= current_user.email %>
      </p>
    </div>
  <% end %>
</div>
