<div class="max-w-6xl mx-auto px-4">
  <%# ══ Hero section ══ %>
  <div class="text-center py-16">
    <h1 class="text-5xl font-bold text-gray-900 mb-4">🧠 QuizApp</h1>
    <p class="text-xl text-gray-500 mb-8 max-w-2xl mx-auto">
      Plataforma de quizzes interactivos. Aprende, desafía tus conocimientos
      y compite por los mejores puntajes.
    </p>

    <div class="flex justify-center gap-4">
      <% if user_signed_in? %>
        <% if current_user.admin? %>
          <%= link_to "Panel Admin", admin_root_path,
              class: "bg-indigo-600 text-white px-8 py-3 rounded-lg hover:bg-indigo-700 font-semibold text-lg" %>
        <% end %>
        <%= link_to "Ver Quizzes", quizzes_path,
            class: "bg-indigo-600 text-white px-8 py-3 rounded-lg hover:bg-indigo-700 font-semibold text-lg" %>
      <% else %>
        <%= link_to "Comenzar", new_user_registration_path,
            class: "bg-indigo-600 text-white px-8 py-3 rounded-lg hover:bg-indigo-700 font-semibold text-lg" %>
        <%= link_to "Iniciar Sesión", new_user_session_path,
            class: "bg-gray-100 text-gray-700 px-8 py-3 rounded-lg hover:bg-gray-200 font-semibold text-lg" %>
      <% end %>
    </div>
  </div>

  <%# ══ Quizzes destacados ══ %>
  <% published_quizzes = Quiz.published.recent.limit(6) %>
  <% if published_quizzes.any? %>
    <div class="mb-16">
      <h2 class="text-2xl font-bold text-gray-900 mb-6 text-center">Quizzes Disponibles</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <% published_quizzes.each do |quiz| %>
          <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden hover:shadow-md transition">
            <% if quiz.cover_image.attached? %>
              <%= image_tag quiz.cover_image, class: "w-full h-40 object-cover" %>
            <% else %>
              <div class="w-full h-40 bg-gradient-to-br from-indigo-400 to-purple-500 flex items-center justify-center">
                <span class="text-5xl">🧠</span>
              </div>
            <% end %>
            <div class="p-5">
              <h3 class="text-lg font-semibold text-gray-900 mb-2"><%= quiz.title %></h3>
              <p class="text-gray-600 text-sm mb-3 line-clamp-2"><%= quiz.description %></p>
              <div class="flex justify-between items-center text-sm text-gray-400 mb-3">
                <span><%= quiz.total_questions %> preguntas</span>
                <span><%= quiz.user.name %></span>
              </div>
              <%= link_to "Ver Quiz →", quiz_path(quiz),
                  class: "block w-full text-center bg-indigo-600 text-white py-2 rounded-lg hover:bg-indigo-700 font-medium text-sm" %>
            </div>
          </div>
        <% end %>
      </div>

      <% if Quiz.published.count > 6 %>
        <div class="text-center mt-8">
          <%= link_to "Ver todos los quizzes →", quizzes_path,
              class: "text-indigo-600 hover:text-indigo-800 font-medium" %>
        </div>
      <% end %>
    </div>
  <% end %>

  <%# ══ Cómo funciona ══ %>
  <div class="py-12 mb-8">
    <h2 class="text-2xl font-bold text-gray-900 mb-8 text-center">¿Cómo funciona?</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <div class="text-center">
        <div class="text-4xl mb-3">📝</div>
        <h3 class="font-semibold text-gray-900 mb-2">Elige un Quiz</h3>
        <p class="text-gray-500 text-sm">Navega por los quizzes disponibles y elige el que más te interese.</p>
      </div>
      <div class="text-center">
        <div class="text-4xl mb-3">🎯</div>
        <h3 class="font-semibold text-gray-900 mb-2">Responde</h3>
        <p class="text-gray-500 text-sm">Selecciona la respuesta correcta para cada pregunta del quiz.</p>
      </div>
      <div class="text-center">
        <div class="text-4xl mb-3">🏆</div>
        <h3 class="font-semibold text-gray-900 mb-2">Ve tu Resultado</h3>
        <p class="text-gray-500 text-sm">Al finalizar, ve tu puntaje y el detalle de cada respuesta.</p>
      </div>
    </div>
  </div>
</div>
